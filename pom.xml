<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.0</version>
        <relativePath/>
    </parent>

    <groupId>com.iambilotta.demo</groupId>
    <artifactId>spring-gdpr-aiact-demo</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>spring-gdpr-aiact-demo</name>
    <description>End-to-end demo of spring-aiact + spring-gdpr running together in a single Spring Boot service.</description>
    <url>https://github.com/iambilotta/spring-gdpr-aiact-demo</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>Francesco Bilotta</name>
            <email>francesco@iambilotta.com</email>
            <url>https://iambilotta.com</url>
        </developer>
    </developers>

    <scm>
        <url>https://github.com/iambilotta/spring-gdpr-aiact-demo</url>
        <connection>scm:git:git@github.com:iambilotta/spring-gdpr-aiact-demo.git</connection>
        <developerConnection>scm:git:git@github.com:iambilotta/spring-gdpr-aiact-demo.git</developerConnection>
    </scm>

    <properties>
        <java.version>21</java.version>
        <spring-aiact.version>v1.0.0</spring-aiact.version>
        <spring-gdpr.version>v1.0.0</spring-gdpr.version>
    </properties>

    <repositories>
        <repository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
        </pluginRepository>
    </pluginRepositories>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>com.github.iambilotta.spring-aiact</groupId>
            <artifactId>spring-aiact-spring-boot-starter</artifactId>
            <version>${spring-aiact.version}</version>
        </dependency>

        <dependency>
            <groupId>com.github.iambilotta.spring-gdpr</groupId>
            <artifactId>spring-gdpr-starter</artifactId>
            <version>${spring-gdpr.version}</version>
        </dependency>
        <dependency>
            <groupId>com.github.iambilotta.spring-gdpr</groupId>
            <artifactId>spring-gdpr-annotations</artifactId>
            <version>${spring-gdpr.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <parameters>true</parameters>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>com.github.iambilotta.spring-gdpr</groupId>
                            <artifactId>spring-gdpr-processor</artifactId>
                            <version>${spring-gdpr.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>

    <profiles>
        <!--
          The spring-aiact Maven plugin can verify the high-risk class annotations and
          generate the Annex IV technical file, the Article 47 DoC PDF and the dataset
          datasheets at `mvn verify`. We cannot wire it into the default reactor here
          because JitPack rewrites the plugin's groupId from `com.iambilotta.spring` to
          `com.github.iambilotta.spring-aiact`, which makes Maven reject the plugin
          descriptor as inconsistent.

          Workaround for users who want the build-time artifacts:

            1. clone https://github.com/iambilotta/spring-aiact next to this repo,
            2. run `( cd ../spring-aiact && ./mvnw -B -DskipTests install )`,
            3. run `mvn -P aiact-build-artifacts verify` from this repo.

          The runtime side (audit chain, REST endpoints, integration tests) does NOT
          need the plugin; it works straight from JitPack.
        -->
        <profile>
            <id>aiact-build-artifacts</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.iambilotta.spring</groupId>
                        <artifactId>spring-aiact-maven-plugin</artifactId>
                        <version>1.0.0</version>
                        <executions>
                            <execution>
                                <id>aiact-verify-and-generate</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>verify</goal>
                                    <goal>generate</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
