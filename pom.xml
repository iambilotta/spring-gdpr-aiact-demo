<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.0</version>
        <relativePath/>
    </parent>

    <groupId>com.iambilotta.demo</groupId>
    <artifactId>iambilotta-compliance-demo</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>iambilotta-compliance-demo</name>
    <description>End-to-end demo: spring-aiact + spring-gdpr in one Spring Boot service.</description>

    <properties>
        <java.version>21</java.version>
        <spring-aiact.version>0.1.1</spring-aiact.version>
        <spring-gdpr.version>0.1.1</spring-gdpr.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>com.iambilotta.spring</groupId>
            <artifactId>spring-aiact-spring-boot-starter</artifactId>
            <version>${spring-aiact.version}</version>
        </dependency>

        <dependency>
            <groupId>com.iambilotta.gdpr</groupId>
            <artifactId>spring-gdpr-starter</artifactId>
            <version>${spring-gdpr.version}</version>
        </dependency>
        <dependency>
            <groupId>com.iambilotta.gdpr</groupId>
            <artifactId>spring-gdpr-annotations</artifactId>
            <version>${spring-gdpr.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <parameters>true</parameters>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>com.iambilotta.gdpr</groupId>
                            <artifactId>spring-gdpr-processor</artifactId>
                            <version>${spring-gdpr.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <plugin>
                <groupId>com.iambilotta.spring</groupId>
                <artifactId>spring-aiact-maven-plugin</artifactId>
                <version>${spring-aiact.version}</version>
                <executions>
                    <execution>
                        <id>aiact-verify-and-generate</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>verify</goal>
                            <goal>generate</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
